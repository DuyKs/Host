<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Page</title>
    <link rel="stylesheet" href="{{ asset('styles/product.css') }}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <header>
        <h1>Product Page</h1>
        <nav>
            <a href="{{ path('app_home') }}"><i class="fas fa-home"></i> Home</a>
            <a href="{{ path('app_product') }}"><i class="fas fa-box"></i> Product</a>
        </nav>
    </header>
    <main>
        <h2>Product List (Total: {{ product_count }})</h2>
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Description</th>
                    <th>Price</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for product in products %}
                    <tr>
                        <td>{{ loop.index }}</td> <!-- Display the product number -->
                        <td>{{ product.name }}</td>
                        <td>{{ product.description }}</td> <!-- Display the product description -->
                        <td>{{ product.price | number_format(2, '.', ',') }}</td>
                        <td>
                            <a href="{{ path('app_product_details', {'id': product.id}) }}" class="button view" title="View Details"><i class="fas fa-eye"></i></a>
                            <a href="{{ path('app_product_edit', {'id': product.id}) }}" class="button edit" title="Edit"><i class="fas fa-edit"></i></a>
                            <form method="post" action="{{ path('app_product_delete', {'id': product.id}) }}" style="display:inline;">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
                                <button type="submit" class="button delete" onclick="return confirm('Are you sure you want to delete this product?');" title="Delete">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </form>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        <a href="{{ path('app_product_new') }}" class="button add"><i class="fas fa-plus"></i> Add New Product</a>
    </main>
    <footer>
        <p>&copy; {{ "now"|date("Y") }} Your Company</p>
    </footer>
</body>
</html>
