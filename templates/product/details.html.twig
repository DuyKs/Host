<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details</title>
    <link rel="stylesheet" href="{{ asset('styles/product.css') }}">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <header>
        <h1>Product Details</h1>
        <nav>
            <a href="{{ path('app_home') }}"><i class="fas fa-home"></i> Home</a>
            <a href="{{ path('app_product') }}"><i class="fas fa-box"></i> Products</a>
        </nav>
    </header>
    <main>
        <div class="product-info">
            <h2>{{ product.name }}</h2>
            <p>{{ product.description }}</p>
            <p class="price">Price: ${{ product.price | number_format(2, '.', ',') }}</p>
            <a href="{{ path('app_product') }}" class="button return"><i class="fas fa-arrow-left"></i> Back to Product List</a>
        </div>
    </main>
    <footer>
        <p>&copy; {{ "now"|date("Y") }} Your Company</p>
    </footer>
</body>
</html>
